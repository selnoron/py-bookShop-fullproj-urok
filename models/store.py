# Local
from book import Book


class Store:
    """Book Store."""

    def __init__(
        self,
        address: str,
        books: list[Book]
    )
        self.address = address
        self.books = books

